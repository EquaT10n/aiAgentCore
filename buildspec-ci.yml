version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - python -m pip install --upgrade pip
      - pip install ruff pytest
      - pip install -e .
  build:
    commands:
      - ruff check .
      - pytest -q
